{
  "_comment": "Thresholds for Llama-3.3-70B BF16 on MI300X single-node. Every metric has a default entry so PASS/FAIL is threshold-driven (requires enforce_thresholds=true). step_time_*/eval_loss/steps_to_target/time_to_target_seconds are lenient placeholders to calibrate; metrics with no value produced this run (e.g. eval_loss, scaling) are reported N/A, not FAIL.",

  "STEPS=30,BATCH=2,SEQLEN=8192,NODES=1": {
    "training.tflops_per_sec_per_gpu": { "kind": "min", "value": 350.0 },
    "training.tokens_per_sec_per_gpu": { "kind": "min", "value": 700.0 },
    "training.tokens_per_sec_total":   { "kind": "min", "value": 0 },
    "training.scaling_efficiency_pct": { "kind": "min", "value": 80.0 },
    "training.step_time_seconds":      { "kind": "max", "value": 3600.0 },
    "training.step_time_mean_ms":      { "kind": "max_ms", "value": 3600000.0 },
    "training.step_time_p50_ms":       { "kind": "max_ms", "value": 3600000.0 },
    "training.step_time_p95_ms":       { "kind": "max_ms", "value": 3600000.0 },
    "training.final_loss":             { "kind": "max", "value": 15.0 },
    "training.loss_decreased":         { "kind": "min", "value": 1 },
    "training.eval_loss":              { "kind": "max", "value": 100.0 },
    "training.steps_to_target":        { "kind": "max", "value": 1000000 },
    "training.time_to_target_seconds": { "kind": "max", "value": 1000000.0 }
  }
}
