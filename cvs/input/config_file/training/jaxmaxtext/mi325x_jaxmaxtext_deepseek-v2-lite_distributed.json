{
  "_format_note": "CVS training-config layout. Top-level order: (1) cvs params at root, (2) container (incl. its env), (3) train_params (canonical keys + framework passthrough under maxtext_config), (4) tests blocks at root, (5) sweeps + runs. run_mode/nnodes/nic_type are handled internally; the NCCL device selection and JAX distributed knobs are exact env-var names inside container.env.",
  "gpu_name": "mi325x",
  "threshold_json": "mi325x_jaxmaxtext_deepseek-v2-lite_distributed_threshold.json",
  "enforce_thresholds": false,
  "gpus_per_node": 8,
  "paths": {
    "shared_fs": "/home/{user-id}",
    "models_dir": "{shared_fs}/cache/maxtext",
    "log_dir": "{shared_fs}/LOGS/jaxmaxtext",
    "hf_token_file": "{shared_fs}/.hf_token",
    "temp_dir": "/tmp/{user-id}/jaxmaxtext"
  },
  "container": {
    "lifetime": "per_run",
    "name": "rocm-jaxmaxtext-mi325x-deepseek-v2-lite",
    "image": "rocm/jax-training:maxtext-v26.4 <changeme>",
    "runtime": {
      "name": "docker",
      "args": {
        "network": "host",
        "ipc": "host",
        "privileged": true,
        "shm-size": "256G",
        "ulimit": [
          "nofile=65535:65535"
        ],
        "volumes": [
          "/home/{user-id}:/home/{user-id}",
          "/dev/infiniband:/dev/infiniband",
          "/usr/local/lib/libbnxt_re-rdmav34.so:/usr/lib/x86_64-linux-gnu/libibverbs/libbnxt_re-rdmav34.so:ro",
          "/usr/lib/x86_64-linux-gnu/libibverbs.so.1:/usr/lib/x86_64-linux-gnu/libibverbs.so.1:ro",
          "/lib/libibverbs.d:/lib/libibverbs.d",
          "/tmp/{user-id}/jax/TRAINING_LOGS:/workspace/maxtext/output"
        ]
      }
    },
    "_env_comment": "Static ENV exported into the container on every node. Per-node/dynamic vars (JAX_COORDINATOR_IP, NNODES, NODE_RANK, JAX_PROCESS_INDEX) and credentials (HF_TOKEN, HF_HOME, LD_LIBRARY_PATH, PYTHONPATH) are injected by the launcher and must NOT be set here. NCCL_IB_*/*_SOCKET_IFNAME/NCCL_IB_GID_INDEX are the cluster-specific RDMA/NIC device selection (shipped as <changeme>; distributed runs hard-exit at config load until set). JAX_COORDINATOR_PORT / JAX_DISTRIBUTED_* are the JAX coordinator knobs.",
    "env": {
      "GPU_MAX_HW_QUEUES": "2",
      "HSA_FORCE_FINE_GRAIN_PCIE": "1",
      "HIP_FORCE_DEV_KERNARG": "1",
      "HSA_NO_SCRATCH_RECLAIM": "1",
      "XLA_PYTHON_CLIENT_MEM_FRACTION": "0.97",

      "NCCL_IB_HCA": "rdma0,rdma1,rdma2,rdma3,rdma4,rdma5,rdma6,rdma7 <changeme>",
      "NCCL_SOCKET_IFNAME": "eno0 <changeme>",
      "GLOO_SOCKET_IFNAME": "eno0 <changeme>",
      "NCCL_IB_GID_INDEX": "3 <changeme>",

      "NCCL_DEBUG": "VERSION",
      "NCCL_IB_DISABLE": "0",
      "NCCL_PROTO": "Simple",
      "NCCL_IB_TC": "41",
      "NCCL_IB_SL": "0",
      "NCCL_CHECKS_DISABLE": "1",
      "NCCL_CROSS_NIC": "0",

      "NVTE_ALLOW_NONDETERMINISTIC_ALGO": "1",
      "NVTE_USE_HIPBLASLT": "1",
      "NVTE_FUSED_ATTN": "1",
      "NVTE_CK_USES_BWD_V3": "1",
      "NVTE_CK_USES_FWD_V3": "1",
      "NVTE_CK_IS_V3_ATOMIC_FP32": "0",
      "NVTE_CK_HOW_V3_BF16_CVT": "2",
      "NVTE_FUSED_ATTN_CK": "1",
      "NVTE_FUSED_ATTN_AOTRITON": "0",

      "JAX_COORDINATOR_PORT": "12346",
      "JAX_DISTRIBUTED_INITIALIZATION_TIMEOUT_SECONDS": "1800",
      "JAX_DISTRIBUTED_HEARTBEAT_TIMEOUT_SECONDS": "900"
    }
  },
  "_train_params_comment": "Canonical training params + framework passthrough. hf_model_id drives the tokenizer download from HuggingFace (skipped for dataset_type=synthetic). model_name (the MaxText preset) also labels the run/report and tokenizer_path is the download target -- both live in maxtext_config. Set an explicit model_id here only if a friendlier CVS label than the preset name is wanted. steps and enable_checkpointing live in maxtext_config. xla_flags stays structured here and is exported as a single XLA_FLAGS string.",
  "train_params": {
    "model_id": "deepseek-v2-lite",
    "hf_model_id": "deepseek-ai/DeepSeek-V2-Lite",
    "_model_name_comment": "DeepSeek-V2-Lite = MaxText preset 'deepseek2-16b' (16B MoE). maxtext_config mirrors MAD scripts/jax-maxtext/env_scripts/deepseek2_16b.yml (gfx942).",
    "train_script_paths": [
      "/workspace/maxtext/src/maxtext/trainers/pre_train/train.py",
      "/workspace/maxtext/src/MaxText/train.py"
    ],
    "maxtext_config": {
      "base_config": "base.yml",
      "model_name": "deepseek2-16b",
      "tokenizer_path": "{paths.models_dir}/DeepSeek-V2-Lite",
      "hardware": "gpu",
      "steps": 30,
      "log_period": 100,
      "enable_checkpointing": false,
      "attention": "cudnn_flash_te",
      "dtype": "bfloat16",
      "dataset_type": "synthetic",
      "remat_policy": "minimal_flash",
      "use_iota_embed": true,
      "scan_layers": true,
      "per_device_batch_size": 10,
      "max_target_length": 4096,
      "async_checkpointing": false,
      "quantization": "",
      "weight_dtype": "bfloat16",
      "shardy": true,
      "sharding_tolerance": 0.05,
      "logits_dot_in_fp32": false,
      "megablox": false,
      "sparse_matmul": false,
      "capacity_factor": 1.25,
      "packing": true,
      "enable_goodput_recording": false,
      "monitor_goodput": false,
      "ici_fsdp_parallelism": 1,
      "ici_data_parallelism": 1,
      "ici_expert_parallelism": -1,
      "dcn_data_parallelism": -1,
      "dcn_fsdp_parallelism": 1,
      "profiler": "",
      "quantize_kvcache": false,
      "kv_quant_axis": "heads_and_dkv",
      "kv_quant_dtype": "int8",
      "checkpoint_is_quantized": false,
      "max_segments_per_seq": 32
    },
    "xla_flags": {
      "xla_gpu_memory_limit_slop_factor": "95",
      "xla_gpu_reduce_scatter_combine_threshold_bytes": "8589934592",
      "xla_gpu_enable_command_buffer": "",
      "xla_gpu_enable_latency_hiding_scheduler": "True",
      "xla_gpu_all_gather_combine_threshold_bytes": "8589934592",
      "xla_gpu_enable_triton_gemm": "False",
      "xla_gpu_enable_cublaslt": "True",
      "xla_gpu_autotune_level": "0",
      "xla_gpu_enable_all_gather_combine_by_dim": "FALSE"
    }
  },
  "_scaling_baseline_comment": "1-node total tokens/sec baseline for scaling-efficiency %. 0.0 = disabled (record-only). Populate from a prior MI325X single-node run (tok/s/GPU * 8) to enable the metric.",
  "scaling_baseline": {
    "tokens_per_sec_total": 0.0,
    "num_nodes": 1
  },
  "_convergence_comment": "to enable validation loss set eval_interval > 0 (and eval_steps) in maxtext_config; needs a validation dataset. target_value <= 0 disables convergence (record-only). target_metric 'auto' uses eval_loss when eval runs, else training loss.",
  "convergence": {
    "target_metric": "auto",
    "target_value": 11.8
  },
  "_loss_curve_comment": "Sample training loss every `sample_every` steps (plus milestone_steps) and pass when the least-squares slope < max_slope. enforce=false makes it record-only.",
  "loss_curve": {
    "sample_every": 2,
    "milestone_steps": [
      100,
      500,
      1000,
      5000
    ],
    "max_slope": 0.0,
    "enforce": true
  },
  "_smoke_comment": "Smoke test (test_smoke), ENABLED by default. Loads the model and runs `steps` steps at a small fixed batch/seqlen in BF16; passes if no error signature fires (no metric checks). A failure gates the rest of the suite. Set enabled=false to SKIP it during iterative experiments (opt-out). steps/per_device_batch_size/max_target_length tune the smoke run.",
  "smoke": {
    "enabled": true,
    "steps": 5,
    "per_device_batch_size": 1,
    "max_target_length": 2048
  },
  "_checkpoint_resume_comment": "Opt-in checkpoint save+resume + I/O-time test. enabled=false -> skipped. Runs ONE sweep twice: train steps_before_ckpt (checkpoint at checkpoint_period), then resume + train steps_after_resume; passes if it restarts from the checkpoint step and the boundary loss matches within loss_tolerance. max_save/load_seconds gate I/O time (0 = record-only). smoke_model_overrides can shrink the model while keeping the tokenizer/vocab; empty = full model. delete_ckpt_dir=true frees disk after the test.",
  "checkpoint_resume": {
    "enabled": false,
    "sweep": "",
    "steps_before_ckpt": 6,
    "steps_after_resume": 6,
    "checkpoint_period": 5,
    "loss_tolerance": 0.1,
    "max_save_seconds": 0.0,
    "max_load_seconds": 0.0,
    "delete_ckpt_dir": true,
    "smoke_model_overrides": {}
  },
  "_error_patterns_comment": "name -> regex scanned in each node's training.log during polling; a match fails that sweep's run with the matched name. Remove this block to use built-in defaults.",
  "error_patterns": {
    "NCCL ERROR": "NCCL ERROR|NCCL timeout|local work queue catastrophic error",
    "GPU HW ERROR": "HW Exception by GPU|GPU Hang|Uncorrectable error|GPU Reset",
    "AssertionError": "AssertionError|ValueError:|JaxStackTrace|During handling of the above exception|triggered the following exception",
    "rocm Err": "FAILED_PRECONDITION: No visible GPU devices|failed call to hipInit: HIP_ERROR_NoDevice|librocm reported version is: NOT_FOUND",
    "python err": "ModuleNotFoundError: No module named|Fatal Python error:",
    "tensorflow": "tensorflow.CoordinationServiceError|tensorflow.BarrierError|CoordinationServiceError",
    "resource": "RESOURCE_EXHAUSTED: Out of memory|failed: RESOURCE_EXHAUSTED",
    "segfault": "Segmentation fault|SIGSEGV|core dumped|std::bad_alloc"
  },
  "_sweeps_comment": "sweeps{} maps a sweep KEY to per-run overrides. The key encodes the primary params and is parsed by CVS into maxtext_config overrides: BS=<per_device_batch_size>, PRECISION=<BF16|FP8> (FP8 -> the GPU's fp8 flavor: nanoo_fp8 on MI300X/MI325X, fp8 on MI350-class), SL=<max_target_length>. The key is also the threshold-file cell key. Add any EXTRA maxtext_config override for a sweep inside its {} (takes precedence over the parsed values), e.g. \"steps\": 300. runs[] selects which sweep keys to execute.",
  "sweeps": {
    "BS=10,PRECISION=BF16,SL=4096": {
      "_comment": "Add extra maxtext_config overrides for this sweep here (BS/PRECISION/SL come from the key), e.g. \"steps\": 300"
    }
  },
  "runs": [
    "BS=10,PRECISION=BF16,SL=4096"
  ]
}
