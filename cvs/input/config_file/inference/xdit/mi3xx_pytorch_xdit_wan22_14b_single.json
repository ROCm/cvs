{
  "_comment": "WAN 2.2 I2V native single-node xDiT workload.",
  "gpu_name" : "<changeme> mi325",
  "enforce_thresholds": true,
  "threshold_json": "mi325_pytorch_xdit_wan22_14b_single_threshold.json",
  "paths": {
    "shared_fs": "{home}",
    "models_dir": "{home}/.cache/huggingface",
    "log_dir": "{home}/cvs_outputs_wan",
    "hf_token_file": "{home}/.hf_token"
  },
  "container": {
    "lifetime": "per_run",
    "name": "wan22-benchmark_single",
    "_image_example": "amdsiloai/pytorch-xdit:v25.11.2",
    "image": "<changeme>",
    "runtime": {
      "name": "docker",
      "args": {
        "network": "host",
        "ipc": "host",
        "privileged": true,
        "shm_size": "128G",
        "volumes": [
          "{paths.models_dir}:/hf_home",
          "{paths.log_dir}:/outputs"
        ],
        "devices": ["/dev/dri", "/dev/kfd"],
        "env": {
          "NCCL_DEBUG": "ERROR"
        }
      }
    }
  },
  "server_params": {
    "backend": "xdit",
    "nnodes": "1",
    "log_level": "info",
    "model": "Wan-AI/Wan2.2-I2V-A14B"
  },
  "benchmark_params": {
    "prompt": "Summer beach vacation style, a white cat wearing sunglasses sits on a surfboard. The fluffy-furred feline gazes directly at the camera with a relaxed expression. Blurred beach scenery forms the background featuring crystal-clear waters, distant green hills, and a blue sky dotted with white clouds. The cat assumes a naturally relaxed posture, as if savoring the sea breeze and warm sunlight. A close-up shot highlights the feline's intricate details and the refreshing atmosphere of the seaside.",
    "size": "720*1280",
    "frame_num": 81,
    "num_benchmark_steps": 5,
    "compile": true,
    "torchrun_nproc": 8
  }
}
