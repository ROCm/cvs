{
    "_comment": "Single-node MI300X cluster for the vllm_single PoC verification. Container backend with launch:true so CVS owns the container lifecycle; the actual image+name come from the variant config.json (container block), with this file providing only the cluster portion (node/user/key). Replace every <changeme> with your node/deployment specifics before running.",

    "orchestrator": "container",
    "username": "{user-id}",
    "priv_key_file": "/data/{user-id}/.ssh/id_ed25519",

    "head_node_dict": {
        "mgmt_ip": "<changeme>"
    },
    "env_vars": {},
    "node_dict": {
        "<changeme>": {
            "bmc_ip": "NA",
            "vpc_ip": "<changeme>"
        }
    },

    "_container_comment": "Stub container block; the testsuite config (variant config.json) overrides image/name/runtime.args via OrchestratorConfig.from_configs.",
    "container": {
        "lifetime": "per_run",
        "name": "<changeme>",
        "image": "<changeme>",
        "runtime": {
            "name": "docker",
            "args": {
                "network": "host",
                "ipc": "host",
                "privileged": true
            }
        }
    }
}
