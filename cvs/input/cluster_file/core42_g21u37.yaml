# Core42 single-node cluster file (pre-DTNI shape).
#
# Pool-level credentials (one SSH key per cluster); per-node entries carry
# only addressing. Device set / NCCL fabric / weka paths live on the
# workload YAML. GPU count is discovered at runtime via rocm-smi by the
# adapter's prepare() phase -- not declared here.
username: atnair
priv_key_file: /data/atnair/.ssh/id_ed25519
env_vars: {}
head_node: 10.245.135.13
nodes:
  10.245.135.13:
    vpc_ip: 10.245.135.13
    bmc_ip: NA
