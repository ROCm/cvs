'''
Suite-specific hook callables referenced from JSON deck profiles.

Metric hooks should point at the suite parsing module (``module:attr`` in profile
JSON). Run-card hooks live here when the default deck run card is insufficient.
'''
